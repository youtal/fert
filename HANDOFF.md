# Handoff

本文档纳入版本管理，用于帮助后续接手者快速理解项目状态、最近的关键改动和当前剩余事项。

## 项目概况

Fret 是一个基于 Vue 3、Vite、Pinia 与 Vitest 的前端项目，当前聚焦三个主要模块：

- 生态系统仿真：粒子、捕食者、空间哈希和后台持续仿真。
- Sudoku 解算：题目生成、自动解算、用户输入反馈与动画展示。
- 点阵网格：150 x 150 点阵、可复现 seed、随机游走网络与滑窗补偿。

## 当前版本

- 应用版本：`0.2.6`

## 最近完成的重点工作

### 生命周期与运行模型

- 所有主 view 都通过 `KeepAlive` 保持会话级连续性。
- 生态系统模块已经重构为“后台仿真推进 + 前台按需渲染”。
- Sudoku 在视图切换后会保留题面、解题进度和自动解算状态。

### Sudoku 相关增强

- 修复了说明面板与 Sidebar 的交互 bug。
- 说明面板与难度面板改为默认折叠、悬浮临时展开、点击锁定展开。
- Sudoku 默认难度调整为 `55`。
- 用户选中数字后，棋盘中其他相同数字会显示增强的字体高亮。
- 用户手动填数时会根据答案副本给出绿色 / 红色反馈。
- 答案副本仅用于用户反馈，自动解算不会读取该副本。
- 自动解算完成时会播放专用完成音效。
- 验证通过时会播放独立的成功音效。

### 2026-03-17 会话补充

- 按用户要求保留 `Vue beta` 技术栈，本轮不触碰依赖版本策略。
- `useSudoku` 中的音频逻辑已拆出为独立模块，主控制器继续保留题目流程与生命周期收口职责。
- 新增全局通知层，正式替代数独流程中的阻塞式 `alert`。
- 生态系统面板与 Sudoku 信息面板已切换到共享浮层壳组件，降低重复样式与重复展开逻辑。
- Dashboard 首页已恢复竖向滚动，同时继续隐藏滚动条，维持原本的海报式视觉效果。
- Sidebar、浮层图标和数独格子补充了更语义化的按钮交互与基础可访问性信息。
- 原先全局隐藏输入光标的做法已收回，避免影响后续真实表单输入。
- 已重新验证：
  - `npm run test:unit -- --run`
  - `npm run type-check`
  - `npm run build`
- 最新测试状态：
  - Test Files: 14 passed
  - Tests: 72 passed

### 2026-04-23 细节优化补充

- GridView 控制面板已复用 `FloatingPanelGroup`，和生态系统 / Sudoku 面板保持一致的“默认折叠、悬浮预览、点击锁定”行为。
- GridView 的点阵网络生成算法已拆出到 `src/utils/gridNetwork.ts`，视图文件只保留 Canvas 绘制、视口交互和面板事件编排。
- 新增 `src/components/grid/GridControlPanel.vue`，种子输入、速度滑块、冻结与重置按钮集中在独立展示组件中。
- 全局 caret / cursor 策略已调整为：非输入区域不显示输入光标，真实文本输入控件恢复文本光标，按钮与滑块保持交互指针。
- 新增 `src/utils/__tests__/gridNetwork.spec.ts`，覆盖 seed 规范化、同 seed 确定性和滑窗补偿追加路径。
- 已尝试执行 `vitest --coverage`，当前项目未安装 `@vitest/coverage-v8`，因此本轮以测试面审计和新增测试文件补强为主，未产出百分比覆盖率报告。
- 最近一次目标测试状态已更新为：
  - Test Files: 15 passed
  - Tests: 75 passed

### 默认首页调整

- 默认首页不再是旧仪表盘。
- 当前首页是瀑布式项目介绍页，集中介绍模块能力、生命周期模型、开发约定和常用命令。

### 工程化整理

- 为核心源码、视图、组件、composables、models、utils 与测试文件补充了更完整的职责说明注释。
- README 已同步更新为当前实现，不再描述未落地能力。
- 类型检查、单测与标准构建都被恢复为可通过状态。

### 2026-03-15 会话补充

- 按用户要求继续补强项目范围内的详细注释，重点覆盖了此前仍较薄弱的函数级说明。
- 本轮特别补充了 `src/composables/useSudoku.ts`、`src/utils/sudoku.ts`、`src/stores/ecosystem.ts` 与 `src/models/Ecosystem.ts` 中的职责说明、调用边界和设计意图注释。
- 版本号已从 `0.2.4` 升级到 `0.2.5`，README 标题也同步更新。
- Sudoku 默认难度已经调整为 `55`。
- 自动解算完成与答案验证通过现在分别使用独立的特殊音效，和普通步进音做区分。
- 默认首页卡片已增加悬浮特效。
- Sudoku 视图在宽度不足时会从左右布局自动切换为上下布局。
- 发布信息已明确写入“粒子演化与数独模块功能冻结本版”。
- 已重新验证：
  - `npm run test:unit -- --run`
  - `npm run type-check`
  - `npm run build`
- 最新测试状态：
  - Test Files: 12 passed
  - Tests: 60 passed

## 当前验证基线

建议每次提交前至少执行以下命令：

```bash
npm run test:unit -- --run
npm run type-check
npm run build
```

最近一次目标状态：

- Test Files: 15 passed
- Tests: 75 passed
- `type-check`: passed
- `build`: passed

## 仓库约定

- 业务逻辑优先收敛到 `composables`、`models`、`utils`。
- 视图和组件主要承担编排与展示职责。
- 任何 Sudoku 用户反馈逻辑都不能反向污染自动解算过程。
- 生态系统模块应持续保持“后台仿真、前台渲染”的职责分离。
- GridView 的网络生成逻辑应继续留在 `utils/gridNetwork.ts`，不要回流到视图组件。
- 除真实文本输入外，静态卡片、棋盘和展示文本不应出现输入光标。
- 进入 `v0.2.6` 后，粒子演化与数独模块仍维持功能冻结，优先进行稳定性、注释、文档、交互一致性和发布整理类修改。

## 当前已知事项

- 远程仓库当前使用 GitHub SSH 地址。
- 机器上已安装 `gh`，但是否已登录取决于当前本地环境。
- 如推送失败，优先检查：

```bash
gh auth status
git remote -v
```

## 继续工作建议

如果下一位接手者要继续推进，建议按这个顺序：

1. 先阅读 `README.md` 和本文件，确认当前实现约束。
2. 执行测试、类型检查和构建，确保本地环境一致。
3. 再进行新功能或 UI 优化，避免破坏 KeepAlive 与后台任务模型。
